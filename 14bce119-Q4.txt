bugged code :

#include<iostream>
#include<stdio.h>

using namespace std;

int f(int x)
{
    if (x != 1 || x != 0)
        return x*f(x-1);
    else
        return 1 ;
}

int main()
{
    int n ;
    cin >> n ;
    cout << f(n);
}

correct code :

#include<iostream>
#include<stdio.h>

using namespace std;

int f(int x)
{
    if (x > 0)
        return x*f(x-1);
    else
        return 1 ;
}

int main()
{
    int n ;
    cin >> n ;
    cout << f(n);
}


bugged line no.: 8